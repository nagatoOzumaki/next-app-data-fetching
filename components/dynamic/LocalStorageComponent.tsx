import { Typography, Container } from '@mui/material';

function LocalStorageComponent() {
  return (
    <Container>
      <Typography>Dynamically imported Component</Typography>
    </Container>
  );
}

export default LocalStorageComponent;
